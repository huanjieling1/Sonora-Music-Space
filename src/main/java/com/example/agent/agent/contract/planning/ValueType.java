package com.example.agent.agent.contract.planning;

/** Planner-facing value types used before capability JSON Schemas are introduced. */
public enum ValueType {
    STRING,
    INTEGER,
    DECIMAL,
    BOOLEAN,
    OBJECT,
    ARRAY,
    ENTITY,
    ANY
}
