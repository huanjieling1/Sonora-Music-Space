package com.example.agent.agent.contract.planning;

/** Stable target vocabulary shared by goal decomposition and generic planning. */
public enum GoalTargetType {
    TRACK,
    ARTIST,
    ALBUM,
    PLAYLIST,
    PROFILE,
    CHART,
    QUEUE,
    SEARCH_RESULT,
    RESPONSE,
    NONE
}
