---
name: 音乐播放控制
description: 当用户明确要求播放已经展示的音乐结果，或将最近展示的推荐结果加入播放队列时使用。
---

# 音乐播放控制

1. 只有收到明确的播放或收听命令后才能开始播放；仅搜索的请求绝不能自动播放。
2. 当前没有结果时，先使用 `music-discovery`，再用从 1 开始的结果位置调用 `playRecommendedTrack`。
3. 只有用户要求把最近的全部结果加入队列时，才调用 `queueLatestRecommendations`。
4. 不得虚构结果位置，也不得在工具确认前声称播放或入队成功。
